plugin -i slang

read_slang --std 1800-2017 -DVTPU_PHYSICAL_SRAM_MACROS --top vtpu_pd_full_top -Irtl/common -f physical/rtl_sources.f

hierarchy -check -top vtpu_pd_full_top
proc
opt
memory -nomap
opt
stat
write_verilog physical/reports/vtpu_pd_full_synth_check_slang.v
